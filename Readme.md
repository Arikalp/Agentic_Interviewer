# Intervo

Live app: [https://intervoo.vercel.app/](https://intervoo.vercel.app/)

Intervo is an AI-powered mock interview platform built with Next.js. It helps users upload a resume, generate tailored interview questions, practice realistic video interviews, and review performance insights with AI-driven feedback.

## What It Does

- Resume-based interview question generation
- Real-time AI interview experience with video, voice, and dynamic recording timer
- Interview transcription, evaluation, and scoring endpoints
- Personalized performance insights and improvement guidance
- On-device facial expression analysis with confidence/anxiety signals per answer
- Authentication-driven dashboard, profile, signup, and login flows

## Tech Stack

- Next.js 16
- React 19
- TypeScript
- Tailwind CSS 4
- Clerk for authentication
- MongoDB for persistence
- Groq-powered AI workflows
- Framer Motion for UI animations
- Vercel Analytics for usage tracking
- MediaPipe Tasks Vision (Face Landmarker)
- ONNX Runtime Web (emotion inference)

## Getting Started

Install dependencies and start the dev server:

```bash
npm install
npm run dev
```

Open [http://localhost:8000](http://localhost:8000) in your browser.

## Behavior Analysis Assets

Behavior analysis runs fully on-device. You need to provide the model assets in public/.

Required files:


The WASM assets are copied from node_modules:


If you swap the emotion model, update the expected input shape and label order in lib/behavior-analysis.ts.

Recent behavior-analysis improvements
- The face landmarker now receives ImageData (prevents runtime crashes when passing a canvas directly).
- Emotion model output is softmaxed and converted to JS numbers before aggregation.
- Confidence scoring was tightened (penalties for low face presence and negative emotions) and exposed as `confidenceScore` (0-100).
- Descriptive outputs added: `confidenceLabel` (e.g. "Very confident") and `confidenceSummary` (short sentence explaining the score) are now returned with `BehaviorMetrics`.
- Development-only MediaPipe log filtering is installed to reduce noisy console messages during dev.


## Available Scripts

```bash
npm run dev
npm run build
npm run start
npm run lint
npm run format
npm run check
```

## Environment Variables

The app expects these environment variables:

- `MONGODB_URI`
- `MONGODB_DB_NAME`
- `GROQ_API_KEY`
- `GROQ_MODEL`

## Main Routes

- `/` landing page
- `/login` sign in
- `/signup` sign up
- `/interview` interview experience
- `/dashboard` user dashboard
- `/profile` user profile
- `/features`, `/demo`, `/how-it-works`, `/insights` marketing sections

## API Routes

- `/api/resume`
- `/api/interview/questions`
- `/api/interview/transcribe`
- `/api/interview/evaluate`
- `/api/interview/scores`

## Project Structure

- `app/` Next.js app router pages and API routes
- `components/landing/` landing page sections and UI
- `components/ui/` reusable UI primitives
- `lib/` MongoDB, resume parsing, and AI helpers

## Deployment

The project is deployed on Vercel and can be accessed here:

- [https://intervoo.vercel.app/](https://intervoo.vercel.app/)

## PWA (Progressive Web App)

- A web manifest is available at `/manifest.webmanifest` (generated by `app/manifest.ts`).
- A service worker lives at `public/sw.js` and is registered in production from `app/layout.tsx` (registers `/sw.js`).
- The PWA uses the site's `favicon.ico` as the app icon by default; if you want larger or maskable icons, add them to `public/` and update `app/manifest.ts`.

## Recent UX & Build Notes

- Behavior snapshot moved into each answer card in the interview review UI (right-side answer panel).
- Recording countdown/time-left is shown both in the left status panel and near the recording button.
- Question TTS has improved cancellation/flush logic to avoid trailing audio from previous utterances.
- TypeScript/build: fixed manifest typing and other TS issues; run `npm run build` to verify a production build.

If you'd like me to expand any of these sections (deployment tips, CI/CD, or icon recommendations for Android/iOS), tell me which area to document next.
